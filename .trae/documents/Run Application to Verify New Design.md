1. **Launch Application**: Start the app using `flutter run` (or Hot Restart if active) to apply the latest design changes.
2. **Verify "Glassmorphism" UI**: Confirm that the Bottom Navigation Bar and Search Bar have the frosted glass (blur) effect and correct transparency.
3. **Verify Gradient**: Check that the center "+" button displays the new **Blue-Green (Logo V2)** gradient (`#2E3192` -> `#1BFFFF`).

